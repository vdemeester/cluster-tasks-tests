apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  generateName: s2i-go-run-
spec:
  pipelineRef:
    name: s2i-go-pipeline
  timeout: 10m
  workspaces:
  - name: source
    persistentVolumeClaim:
      claimName: shared-pvc