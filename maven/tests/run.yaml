apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  generateName: maven-run-
spec:
  pipelineRef:
    name: maven-pipeline
  workspaces:
  - name: source
    persistentvolumeclaim:
      claimName: shared-pvc
  - name: maven-settings
    emptyDir: {}
