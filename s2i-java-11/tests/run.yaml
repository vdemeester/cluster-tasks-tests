apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  generateName: s2i-java-11-run-
spec:
  pipelineRef:
    name: s2i-java-11-pipeline
  workspaces:
  - name: source
    persistentvolumeclaim:
      claimName: shared-pvc