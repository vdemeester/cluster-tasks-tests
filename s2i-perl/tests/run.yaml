apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  generateName: s2i-perl-run-
spec:
  pipelineRef:
    name: s2i-perl-pipeline
  workspaces:
  - name: source
    persistentvolumeclaim:
      claimName: shared-pvc