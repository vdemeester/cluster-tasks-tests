apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  generateName: s2i-python-3-run-
spec:
  pipelineRef:
    name: s2i-python-3-pipeline
  workspaces:
  - name: source
    persistentvolumeclaim:
      claimName: shared-pvc