apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: s2i-python-3-pipeline
spec:
  params:
  - name: MINOR_VERSION
    description: minor version of Python 3
  - name: SUBDIR
    description: where to clone the git repo
    default: s2i-python-3
  workspaces:
  - name: source
  tasks:
  - name: clone-git-repo
    taskRef:
      name: git-clone
      kind: ClusterTask
    workspaces:
    - name: output
      workspace: source
    params:
    - name: url
      value: https://github.com/sclorg/django-ex
    - name: subdirectory
      value: $(params.SUBDIR)
    - name: deleteExisting
      value: "true"
  - name: build
    taskRef:
      name: s2i-python-3
      kind: ClusterTask
    runAfter: 
    - clone-git-repo
    workspaces:
    - name: source
      workspace: source
    params:
    - name: IMAGE
      value: image-registry.openshift-image-registry.svc:5000/catalog-tests/s2i-python-3$(params.MINOR_VERSION)
    - name: MINOR_VERSION
      value: $(params.MINOR_VERSION)
    - name: PATH_CONTEXT
      value: $(params.SUBDIR)
    - name: TLSVERIFY
      value: "false"
