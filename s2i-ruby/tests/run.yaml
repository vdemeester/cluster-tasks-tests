apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  generateName: s2i-ruby-run-
spec:
  pipelineRef:
    name: s2i-ruby-pipeline
  workspaces:
  - name: source
    persistentvolumeclaim:
      claimName: shared-pvc