apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  generateName: s2i-java-8-run-
spec:
  pipelineRef:
    name: s2i-java-8-pipeline
  timeout: 15m
  workspaces:
  - name: source
    persistentVolumeClaim:
      claimName: shared-pvc