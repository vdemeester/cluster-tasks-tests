apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  generateName: s2i-dotnet-3-run-
spec:
  pipelineRef:
    name: s2i-dotnet-3-pipeline
  timeout: 10m
  workspaces:
  - name: source
    persistentvolumeclaim:
      claimName: shared-pvc