---
apiVersion: tekton.dev/v1alpha1
kind: TaskRun
metadata:
  name: git-clone-run
spec:
  taskRef:
    name: git-clone
    kind: ClusterTask
  params:
    - name: url
      value: "https://github.com/che-samples/console-java-simple"
  workspaces:
    - name: output
      persistentVolumeClaim:
        claimName: jib-maven-pvc
 
